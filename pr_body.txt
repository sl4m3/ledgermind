💡 What: Added an actionable toast notification when the 'ledgermind-mcp' CLI is missing.
🎯 Why: Previously, users were only given a red status bar without a clear explanation if the CLI dependency was missing. This makes the error more explicit and actionable.
📸 Before/After: N/A - Visual toast notification addition.
♿ Accessibility: Improves user feedback by providing explicit text notifications for critical background process failures, which screen readers will announce immediately compared to passive status bar color changes.
