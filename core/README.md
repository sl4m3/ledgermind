# agent-memory-core v1.3.0-evolution

Универсальный модуль долгосрочной памяти для ИИ-агентов с гарантированной целостностью.

## 🛡 Гарантии целостности

- **ACID на уровне файлов**: Файловые блокировки предотвращают Race Condition, а Git обеспечивает атомарность и версионирование.
- **Моногенные связи**: Каждая ссылка (`supersedes` / `superseded_by`) проверяется в обе стороны при каждом запуске. Битая ссылка = System Halt.
- **Бессмертные доказательства**: Эпизоды (Episodic Memory), послужившие основой для семантических решений, защищены от удаления Decay Engine (I6 Invariant).
- **I7: The Hypothesis Invariant**: Reflection Engine может создавать только гипотезы (`kind: proposal`). Никакая автоматика не может изменять "истину" (decisions) напрямую.
- **Recovery Engine**: Автоматическое обнаружение и фиксация файлов, оставшихся после системных сбоев.

## 🚀 Основные функции

- **Knowledge Evolution**: Строгий процесс замещения старых знаний новыми через DAG-структуру.
- **Hybrid Search**: Сочетание графовых связей (Git) и семантического поиска (Vector Index на SQLite).
- **Mature Reflection**: Двигатель саморефлексии, анализирующий ошибки и успехи для генерации предложений (Proposals) с учетом инертности и затухания уверенности.
- **Episodic Decay**: Автоматическая архивация и очистка старых событий, не связанных с важными решениями.

## 🛠 Архитектура

- **Semantic Store**: Markdown + YAML Frontmatter, хранится в Git.
- **Episodic Store**: SQLite, append-only лог событий.
- **Vector Store**: SQLite + Embeddings, поисковый слой для навигации.
- **Reflection Engine**: Анализатор паттернов и генератор гипотез.
- **Conflict & Resolution Engines**: Логика обнаружения и разрешения противоречий.

## ⚡ Быстрый старт

```python
from api.memory import Memory

memory = Memory(storage_path="./my_agent_memory")

# Запись решения
memory.record_decision(
    title="Use PostgreSQL",
    target="database",
    rationale="Need ACID and scaling"
)

# Семантический поиск
results = memory.search_decisions("How do we store data?")
```
