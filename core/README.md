# agent-memory-core v1.14.0

Универсальный модуль долгосрочной памяти для ИИ-агентов с гарантированной целостностью.

## 🛡 Гарантии целостности

- **ACID-compliant Transactions**: Поддержка атомарных операций через `SemanticStore.transaction()`. Сложные изменения (например, `supersede`) либо применяются полностью, либо не применяются вовсе.
- **Proactive Validation**: Валидация инвариантов (`IntegrityChecker`) выполняется **ДО** фиксации изменений в Git.
- **Hybrid Locking**: Разделяемые блокировки (Shared) для читателей и эксклюзивные (Exclusive) для писателей предотвращают чтение несогласованных данных.
- **Graph-First Hybrid Search**: Жесткая политика разрешения конфликтов. Активные знания всегда выше вытесненных, автоматическая дедупликация результатов по целям (targets).
- **Acyclic Knowledge Graph**: Строгая проверка на отсутствие циклов в цепочках вытеснения знаний.
- **Scientific Falsification**: Механизм автоматического опровержения гипотез при обнаружении противоречащих улик.

## 🚀 Основные функции

- **Competitive Hypotheses Model**: Система рефлексии больше не создает одиночные правила. Она генерирует конкурирующие объяснения ошибок, которые соревнуются за уверенность на основе новых данных.
- **Bayesian Confidence Scoring**: Расчет уверенности в знаниях, который учитывает как подтверждающие, так и опровергающие события.
- **Git History Indexing**: Автоматическая индексация коммитов проекта.

- **Competitive Reflection & Falsification**: Механизм рефлексии нового поколения, предотвращающий петли самоподтверждения через учет отрицательных улик и штрафы за ложноположительные гипотезы.
- **Knowledge Evolution**: Строгий процесс замещения старых знаний новыми через DAG-структуру.
- **Recovery Engine**: Автоматическое обнаружение и фиксация файлов, оставшихся после системных сбоев.

## 🚀 Основные функции

- **Knowledge Evolution**: Строгий процесс замещения старых знаний новыми через DAG-структуру. Каждое изменение — это коммит в Git.
- **Git History Indexing**: Автоматическая индексация коммитов проекта для учета изменений кода в процессе рефлексии.
- **Procedural Distillation (MemP)**: Автоматическое превращение успешных цепочек событий (траекторий) в структурированные процедуры и SOP (Standard Operating Procedures).
- **Hybrid Search**: Сочетание графовых связей (Git/YAML) и семантического поиска (Vector Index на SQLite).
- **Mature Reflection**: Двигатель саморефлексии, анализирующий ошибки и успехи для генерации предложений (Proposals) с учетом инертности и затухания уверенности.
- **Episodic Decay**: Автоматическая архивация и очистка старых событий, не связанных с важными решениями.

## 🛠 Архитектура

- **Semantic Store**: Markdown файлы с YAML Frontmatter. Каждое решение хранится как отдельный файл, версионируемый через встроенный Git-репозиторий.
- **Episodic Store**: SQLite БД для хранения append-only лога событий. Поддерживает статусы `active`, `archived` и `pruned`.
- **Vector Store**: Обеспечивает семантический поиск по содержимому семантической памяти.
- **Reasoning Engines**:
    - **Conflict Engine**: Обнаруживает противоречивые решения для одного и того же объекта (target).
    - **Resolution Engine**: Проверяет корректность разрешения конфликтов.
    - **Decay Engine**: Управляет жизненным циклом эпизодической памяти.
    - **Distillation Engine**: Извлекает процедурные знания из успешного опыта.

## ⚡ Быстрый старт

```python
from agent_memory_core.api.memory import Memory

memory = Memory(storage_path="./my_agent_memory")

# Запись решения
memory.record_decision(
    title="Use PostgreSQL",
    target="database",
    rationale="Need ACID and scaling"
)

# Семантический поиск
results = memory.search_decisions("How do we store data?")
```
