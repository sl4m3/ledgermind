# agent-memory-core v1.15.0

Универсальный модуль долгосрочной памяти для ИИ-агентов с гарантированной целостностью и эпистемическим моделированием знаний.

## 🛡 Гарантии целостности

- **ACID-compliant Transactions**: Поддержка атомарных многофайловых операций через `SemanticStore.transaction()`. Изменения либо применяются полностью, либо не применяются вовсе при нарушении инвариантов.
- **Proactive Validation**: Валидация всех архитектурных инвариантов (`IntegrityChecker`) выполняется **ДО** фиксации изменений в Git.
- **Hybrid Locking**: Система разделяемых (Shared) и эксклюзивных (Exclusive) блокировок на уровне ОС предотвращает конкурентные ошибки записи и чтение "грязных" данных.
- **Graph-First Hybrid Search**: Векторный поиск фильтруется через семантический граф. Активные знания всегда имеют приоритет, реализована автоматическая дедупликация результатов по целям (targets).
- **Acyclic Knowledge Graph**: Строгая проверка на отсутствие циклов в цепочках вытеснения (supersede) знаний.

## 🚀 Основные функции

- **Epistemic Reflection & Falsification**: Система рефлексии нового поколения, которая не просто считает ошибки, а строит эпистемическую модель:
    - **Competing Hypotheses**: Для каждого паттерна ошибок генерируются альтернативные объяснения, которые конкурируют за уверенность.
    - **Structured Scrutiny**: Каждое предложение (Proposal) включает `strengths` (сильные стороны), `objections` (возражения) и `counter-patterns`.
    - **Active Falsification**: Успешные операции в проблемной области трактуются как активные возражения, снижающие уверенность в гипотезе вплоть до её полной фальсификации.
- **Procedural Distillation (MemP)**: Автоматическое извлечение процедурных знаний (SOP) из успешных цепочек событий (траекторий).
- **Git History Indexing**: Обогащение памяти знаниями из истории коммитов человеческого кода.
- **Knowledge Evolution**: Строгий процесс замещения старых знаний новыми через DAG-структуру с использованием Git для аудита.
- **Episodic Decay**: Умная очистка и архивация эпизодической памяти на основе Bayesian-рейтингов важности.

## 🛠 Архитектура

- **Semantic Store**: Markdown-файлы с YAML Frontmatter, версионируемые через Git. Основное хранилище "истин" и правил.
- **Episodic Store**: SQLite БД для потока событий (append-only log).
- **Vector Store**: Индекс для быстрого поиска кандидатов по семантической близости.
- **Reasoning Engines**: Набор специализированных двигателей (Reflection, Conflict, Distillation, Decay) для обработки накопленного опыта.

## ⚡ Быстрый старт

```python
from agent_memory_core.api.memory import Memory

# Инициализация (с поддержкой транзакций и блокировок)
memory = Memory(storage_path="./my_agent_memory")

# Запись решения (автоматическая фиксация в Git)
memory.record_decision(
    title="Use PostgreSQL",
    target="database",
    rationale="Need ACID and vertical scaling for initial phase"
)

# Гибридный поиск (с дедупликацией и приоритетом активных знаний)
results = memory.search_decisions("How to store structured data?", mode="balanced")
```
