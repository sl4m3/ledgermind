# Agent Memory Multi v1.1.4

Универсальный мост (Bridge) между ядром памяти `agent-memory-core` и различными LLM-провайдерами. Пакет предоставляет инструменты для интеграции памяти в рабочие процессы агентов через стандартные протоколы и фреймворки.

## 🚀 Основные возможности

- **MCP (Model Context Protocol)**: Полноценный сервер памяти для Claude Desktop и других MCP-клиентов. Позволяет любой модели использовать память как набор инструментов.
- **Мульти-провайдерная поддержка**: Адаптеры для OpenAI, Anthropic, Google (Gemini) и Ollama. Автоматическая генерация схем инструментов.
- **Resilient Embeddings**: Отказоустойчивая система эмбеддингов с автоматическим переключением (например, OpenAI -> Ollama -> Mock).
- **Environment Context**: Инструменты для захвата снимков окружения (файлы, Git, переменные) в эпизодическую память для последующего анализа.
- **Интеграция с фреймворками**: Готовые инструменты для LangChain и CrewAI.

## 📂 Структура проекта

- `agent_memory_multi/adapters/`: Адаптеры для API провайдеров и реализация MCP сервера.
- `agent_memory_multi/frameworks/`: Обертки для AI-фреймворков и сборщик контекста окружения.
- `agent_memory_multi/embeddings.py`: Логика работы с векторными вложениями и Fallback-система.
- `agent_memory_multi/manager.py`: Центральный контроллер `MemoryMultiManager` для координации вызовов.
- `agent_memory_multi/cli.py`: Точка входа для запуска MCP сервера из командной строки.

## 🛠 Использование

### Подключение через MCP (Claude Desktop и др.)

Добавьте следующую конфигурацию в ваш файл настроек MCP (например, `claude_desktop_config.json`):

```json
{
  "general": {
    "previewFeatures": true
  },
  "mcpServers": {
    "agent-memory": {
      "command": "agent-memory-mcp",
      "args": [
        "--path",
        ".agent_memory",
        "--name",
        "AgentMemory"
      ]
    }
  }
}
```

### Запуск MCP Сервера из Python

```python
from agent_memory_multi.adapters.mcp_adapter import MCPMemoryAdapter
MCPMemoryAdapter.serve(storage_path=".agent_memory")
```

### Использование контекста окружения
```python
# Агент может вызвать этот инструмент перед выполнением задачи
manager.handle_tool_call("capture_context", {"label": "before_migration"})
```

### Настройка Fallback Embeddings
```python
from agent_memory_multi.embeddings import FallbackEmbeddingProvider, OpenAIEmbeddingProvider, OllamaEmbeddingProvider

provider = FallbackEmbeddingProvider([
    OpenAIEmbeddingProvider(),
    OllamaEmbeddingProvider()
])
memory = Memory(storage_path="./mem", embedding_provider=provider)
```

## 🧪 Тестирование

```bash
pytest multi/tests/
```

## 📝 Лицензия
MIT
