# Agent Memory Multi

Универсальный мост (Bridge) между ядром памяти `agent-memory-core` и различными LLM-провайдерами. Позволяет агентам сохранять стратегические решения в долгосрочную семантическую память независимо от используемой модели или фреймворка.

## 🚀 Основные возможности

- **Мульти-провайдерная поддержка**: Готовые адаптеры для OpenAI, Anthropic (Claude), Google (Gemini) и Ollama.
- **Интеграция с фреймворками**: Инструменты для LangChain и CrewAI "из коробки".
- **Валидация и безопасность**: Строгая проверка аргументов перед записью в память и безопасная обработка некорректных ответов LLM.
- **Асинхронность**: Поддержка асинхронных вызовов в инструментах LangChain.
- **Гибкие схемы**: Автоматическая генерация JSON Schema под требования конкретного провайдера (например, `input_schema` для Anthropic).

## 📂 Структура проекта

- `adapters/`: Специфические реализации для API разных провайдеров.
- `frameworks/`: Обертки для использования памяти как инструментов (Tools) в AI-фреймворках.
- `prompts/`: Оптимизированные системные инструкции для обучения моделей работе с памятью.
- `manager.py`: Центральный контроллер `MemoryMultiManager`.
- `schema.py`: Генератор схем инструментов.

## 🛠 Использование

### Базовый пример с OpenAI

```python
from manager import MemoryMultiManager
from adapters import OpenAIAdapter

# Инициализация (требует объект core из agent-memory-core)
manager = MemoryMultiManager(core_memory)
adapter = OpenAIAdapter(manager)

# Получение определений инструментов для API
tools = adapter.get_tool_definitions()

# Обработка ответа от LLM
results = adapter.process_tool_calls(response.tool_calls)
```

### Интеграция с LangChain

```python
from frameworks import get_langchain_tools

tools = get_langchain_tools(manager)
# Теперь инструменты можно передать в LangChain Agent
```

## 🧪 Тестирование

Для проверки работы всех адаптеров в режиме имитации (mock) запустите:
```bash
python3 -m agent-memory-multi.example_usage
```

## 📝 Лицензия

MIT
